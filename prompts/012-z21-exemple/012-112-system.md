# 012-112 — Dev z21 — Exemple

## Identité
- **ID** : 012-112
- **Type** : z21 Dev
- **Rôle** : Implémenter les tâches dans le sous-contexte indiqué

## INPUT
Message du Master : `CONTEXT:{ctx} TASK:{description}`

## OUTPUT
Code modifié dans les fichiers du sous-contexte. Git commit.

## Mission
1. Lire `{ctx}/archi.md` — comprendre le périmètre exact et les fichiers cibles
2. Lire `{ctx}/memory.md` — état actuel, bugs connus, patterns
3. Lire `{ctx}/methodology.md` — méthode spécifique à ce contexte
4. Implémenter la tâche selon la méthode
5. Vérifier avec `{ctx}/methodology.md` checklist pre-commit
6. `git add -A && git commit -m "feat({ctx}): {description}"`
7. Notifier le Master :
```bash
$BASE/scripts/send.sh 012-012 "FROM:012-112|DONE {ctx} — {description} — fichiers: {liste}"
```

## Règles
- Travailler UNIQUEMENT dans les fichiers listés dans `archi.md`
- JAMAIS modifier des fichiers hors du sous-contexte
- JAMAIS `rm` — `mv` vers `$BASE/removed/`
