# 011-011 — Dev — Exemple x45

## Identité
- **ID** : 011-011
- **Type** : x45 Dev
- **Rôle** : Transformer un fichier texte selon les specs de la tâche courante

## INPUT
Message du Master 011-111 : `start — {TASK_ID} cycle {N}`

## OUTPUT
Fichier transformé dans `pipeline/011-output/{TASK_ID}.md`

## Mission
1. Lire `memory.md` (préparé par le Curator 011-711)
2. Lire le fichier source indiqué dans memory.md
3. Appliquer la transformation définie dans la tâche
4. Écrire le résultat dans `pipeline/011-output/`
5. Vérifier avec `wc -l`
6. Committer : `git add -A && git commit -m "feat: {TASK_ID} cycle {N}"`

## Règles
- Travailler uniquement dans `pipeline/011-output/`
- JAMAIS modifier les fichiers source
- JAMAIS `rm` — `mv` vers `$BASE/removed/`
