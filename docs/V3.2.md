# Multi-Agent v3.2 — preuve des boucles auto-améliorantes

## Évolution v3.2.X des prompts

La ligne v3.2.X introduit une écriture résultat-first : 70 % résultat métier,
20 % vérification, 10 % processus. Les créateurs 150/160/170, les templates et
les exemples suivent ce contrat. `upgrade.sh` migre aussi les `system.md` projet
existants de façon idempotente et récupérable.

Voir [HOW TO WRITE AND REWRITE PROMPTS](<HOW TO WRITE AND REWRITE PROMPTS.md>).

La version 3.2 étend la preuve du bridge aux modes x45/z21 et fournit des
observateurs paramétriques. Contradictor occupe un slot `NNN-2XX` ; l'Ami
expérimental de l'étude n'est pas retenu dans la topologie livrée.

Dans x45/z21, Contradictor est un satellite local du triangle et son contrôle apparaît
dans le diagramme de ce triangle, jamais dans un panneau global. Le format mono
v3.2 est une paire : principal `3XX-1XX` et Contradictor `3XX-2XX`.

## Couverture

| Exigence | Implémentation |
|---|---|
| D1 / R4 | Détection des littéraux recopiés depuis l'oracle, fichiers non suivis inclus |
| D2 | Tokens, coût et temps de verify dans WAL/banc ; absence conservée à `null` |
| D3 | Clone shallow par run, manifeste de harnais, comparaisons incompatibles refusées |
| D4 / C1 | Contrats `spec`, `inventory`, `memory` et `verify_cmd` x45 |
| C2 | Feedback structuré avec preuve et contre-exemple |
| C3 | Outil disponible : règles delta helpful/harmful et pruning traçable ; orchestration automatique à brancher |
| C4 | Outil disponible : admission appariée IC95 ; déclenchement automatique à brancher |
| C5 | Banc disponible : quatre bras d'ablation ; campagnes à lancer explicitement |
| C6 | Primitive disponible : promotion après preuve sur deux triangles ; appel automatique à brancher |
| C7 | Variantes baseline, no-curator et coach-batched ; pas de boucle de recherche automatique |
| G4/G5 | Contradictor intégré ; Ami expérimental et double observateur non retenus |

## Installation nouvelle ou existante

`upgrade.sh` installe les capacités, mais ne crée aucun agent projet. L'opérateur
choisit les slots :

```bash
python3 scripts/scaffold-observers.py 427 \
  --directory-name 427-mon-projet \
  --contradictor-suffix 217 --contradictor-login login3a
```

Le scaffolder archive tout homonyme dans `removed/`, crée prompts, liens de
configuration et workflow matérialisé. Il ne démarre aucun service et
n'authentifie aucun compte.

Pour convertir le mono initial créé par 150 en mono-pair :

```bash
python3 scripts/scaffold-mono-pair.py 345 \
  --directory-name 345-mon-agent \
  --main-suffix 145 --contradictor-suffix 245
```

## Outils

```bash
python3 scripts/agent-bridge/verifier.py contract spec chemin/spec.md
python3 bench/methodology_gate.py ACTIVE CANDIDATE REF CAND --agent NNN-8XX
python3 bench/ablation.py --help
python3 scripts/agent-bridge/orchestrator.py x45-cycle-NNN --variant no-curator
python3 scripts/configure-x45-models.py NNN --prompt-dir NNN-mon-projet
./scripts/echo-decision.sh NNN-2XX followed TASK-42 verify_green
```

Les affectations de modèles restent des hypothèses à comparer à budget égal.
L'isolation réseau n'est déclarée vraie que si le CLI tourne réellement sous un
wrapper réseau ; le framework ne fabrique jamais cette mesure.

```bash
./bench/start-sealed-agent.sh 300
BENCH_REQUIRE_SEALED_NET=1 ./bench/run.sh bras-A 5 heldout v3
```
